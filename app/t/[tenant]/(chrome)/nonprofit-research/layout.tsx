/**
 * The advisor routes of the donor-research section: index, onboarding, reports,
 * personas, diligence templates. They keep the app navbar and footer, so they
 * live in `(chrome)`.
 *
 * The section layout itself is shared with the `(bare)` half — see
 * `donor-research-section-layout.tsx` for why it is not a route file.
 */
export { DonorResearchSectionLayout as default } from "@/src/features/donor-research/components/common/donor-research-section-layout";
