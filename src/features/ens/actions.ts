// ENS-related server actions

export async function resolveENSName(address: string) {
  // Server action for resolving ENS names
  // To be implemented when moving to server components
}

export async function resolveENSAvatar(address: string) {
  // Server action for resolving ENS avatars
  // To be implemented when moving to server components
}

export async function lookupAddress(ensName: string) {
  // Server action for reverse ENS lookup
  // To be implemented when moving to server components
}