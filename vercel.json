{
  "git": {
    "deploymentEnabled": {
      "main": false,
      "*": false
    }
  },
  "buildCommand": "export NODE_OPTIONS='--max-old-space-size=4096'; timeout -k 30s 900s pnpm build || ( rm -rf .next && timeout -k 30s 900s pnpm build )",
  "crons": [
    {
      "path": "/api/cron/warm-sitemaps",
      "schedule": "0 */6 * * *"
    }
  ]
}
