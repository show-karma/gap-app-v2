NEXT_PUBLIC_ENV=
# Origin of the GitBook-hosted product docs. Defaults to
# https://docs.gap.karmahq.xyz; set only if the docs site moves.
NEXT_PUBLIC_DOCS_ORIGIN=
NEXT_PUBLIC_GAP_INDEXER_URL=
NEXT_PUBLIC_ALCHEMY_KEY=
NEXT_PUBLIC_PROJECT_ID=
NEXT_PUBLIC_MIXPANEL_KEY=
NEXT_PUBLIC_GA_TRACKING_ID=
NEXT_PUBLIC_RPC_MAINNET=
NEXT_PUBLIC_RPC_OPTIMISM=
NEXT_PUBLIC_RPC_ARBITRUM=
NEXT_PUBLIC_RPC_CELO=
NEXT_PUBLIC_RPC_BASE=
NEXT_PUBLIC_RPC_SEI=
NEXT_PUBLIC_RPC_BASE_SEPOLIA=
NEXT_PUBLIC_RPC_OPTIMISM_SEPOLIA=
NEXT_PUBLIC_RPC_SEPOLIA=
NEXT_PUBLIC_IPFS_TOKEN=
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_VERCEL_ENV=
NEXT_PUBLIC_PRIVY_APP_ID=
NEXT_PUBLIC_ALCHEMY_POLICY_ID=
NEXT_PUBLIC_AMPLITUDE_KEY=
NEXT_PUBLIC_RPC_LISK=
NEXT_PUBLIC_RPC_SCROLL=
NEXT_PUBLIC_RPC_POLYGON=
NEXT_PUBLIC_E2E_AUTH_BYPASS=false

NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Funding-platform link targets. Leave UNSET to use the runtime-host heuristic:
# same-origin links (/community/<slug>/programs/...) on localhost so local QA stays on
# its own origin, external tenant domains when deployed. Set "true" to force external
# tenant domains (e.g. a deploy preview that should link out), "false" to force
# same-origin links. See utilities/fundingPlatformUrls.ts.
NEXT_PUBLIC_FUNDING_PLATFORM_EXTERNAL_LINKS=

# Shared secret for Vercel Cron requests (vercel.json crons). When set, Vercel
# sends it as 'Authorization: Bearer …' and /api/cron/* routes require it.
CRON_SECRET=

# Sanity Blog CMS. All optional — unset means the blog content gateway
# returns empty results and /blog renders its empty state instead of
# crashing the build. See sanity/env.ts, sanity/lib/client.ts.
# Sanity project id (dashboard.sanity.io > project settings).
NEXT_PUBLIC_SANITY_PROJECT_ID=
# Dataset name. Defaults to "production" when unset.
NEXT_PUBLIC_SANITY_DATASET=
# Sanity API version (date-based). Defaults to "2024-01-01" when unset.
NEXT_PUBLIC_SANITY_API_VERSION=
# Server-only viewer token used to read draft content for the preview flow.
# NEVER expose this to the client — it must stay out of NEXT_PUBLIC_* vars.
SANITY_VIEWER_TOKEN=
# Shared secret verified on incoming Sanity webhook revalidation requests.
SANITY_WEBHOOK_SECRET=
# Shared secret used to authorize the /api/blog/preview draft-mode entry route.
SANITY_PREVIEW_SECRET=

# OAuth authorization server fronting the MCP endpoint, served by gap-indexer.
# Defaults to http://localhost:3003 when unset. See utilities/enviromentVars.ts.
NEXT_PUBLIC_GAP_OAUTH_URL=

# ZeroDev project id for gasless (account-abstraction) attestations. Empty
# string when unset, which disables the gasless path.
NEXT_PUBLIC_ZERODEV_PROJECT_ID=

# Open Source Observer API key, used by the project impact metrics panels.
NEXT_PUBLIC_OSO_API_KEY=

# Hedgey claim-funds provider. CAMPAIGN_IDS is a comma-separated list; NETWORK
# falls back to the tenant's default network when unset.
NEXT_PUBLIC_HEDGEY_CAMPAIGN_IDS=
NEXT_PUBLIC_HEDGEY_NETWORK=

# Extra whitelabel tenant domains injected at deploy time, as a JSON array.
# Domains listed here are absent from DOMAIN_CONFIGS, so getDomainInfo() returns
# undefined for them and callers fall back to the deployment environment.
# Unset means no extra domains. See utilities/whitelabel-config.ts.
WHITELABEL_EXTRA_DOMAINS_JSON=

# Build/tooling only — not needed to run the app locally.
# Firecrawl is used by scripts/generate-llms-txt.ts to scrape the docs site.
# FIRECRAWL_SCRAPE_URL defaults to https://api.firecrawl.dev/v1/scrape.
FIRECRAWL_API_KEY=
FIRECRAWL_SCRAPE_URL=
# Uploads source maps at build time. CI-only; leave unset locally.
SENTRY_AUTH_TOKEN=

# Token bridge (/auth/token-bridge): Vercel preview hostnames of the landing site
# that may be handed a token, matched as https://<prefix>*.vercel.app. Previews
# and local builds only; production ignores it.
NEXT_PUBLIC_TOKEN_BRIDGE_PREVIEW_PREFIX=filpgf-
