export type SortByOptions = "recent" | "completed" | "milestones" | "transactions_asc" | "transactions_desc";
export type StatusOptions = "all" | "to-complete" | "completed" | "starting";
export type Category = {
  id: number;
  name: string;
};
