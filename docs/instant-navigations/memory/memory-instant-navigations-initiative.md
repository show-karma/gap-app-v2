---
name: instant-navigations-initiative
description: "Next 16.3 Instant Navigations adoption for gap-app-v2 — phased plan, briefs in .maestri/briefs, root-layout headers() is the blocker"
metadata: 
  node_type: memory
  type: project
  originSessionId: 87410e44-4932-4121-bd51-44daadc31388
  modified: 2026-08-31T16:33:53.552Z
---

gap-app-v2 is adopting Next.js 16.3 Instant Navigations (`cacheComponents` + `partialPrefetching`, stable since 2026-08-03; app was on 16.2.6). Plan agreed 2026-08-31; task briefs live in `D:\super-gap\.maestri\briefs\instant-nav-task-*.md`.

Phases: (0) land PR #2051 (Suspense streaming for 5 community pages — spike extract) + upgrade to 16.3.x with flags OFF; (1) root-layout refactor — `app/layout.tsx` awaits `getWhitelabelContext()`→`headers()` for whitelabel, which blocks the App Shell for every route; approved fix is Option A: move the read into a Suspense-wrapped `TenantChrome`, theme vars move off `<html style>` (tenant-in-URL Option B deliberately deferred); (2) flip `cacheComponents` and triage all ~156 routes Stream/Cache/Block — crawlable `SITEMAP_NO_LOADING` routes must be **Cache, never Stream** (DEV-612 no-JS rule); token/oauth routes get `export const instant = false`; (3) `partialPrefetching` + selective `<Link prefetch>`; (4) Playwright `instant()` guardrails + rewrite the route-file-structure test premise.

**Why:** navigations block on server fetches today; root-layout headers() makes the whole app dynamic.
**How to apply:** never put `'use cache'` near `TokenManager`-authorized data (cache-poisoning gate); self-hosted standalone deploys need a cacheHandler answer before Phase 2 ships. Status 2026-08-31: PR #2051 all code gates green (FE Dev Alpha fixed two pre-existing suites via shared resolveServerElement helper + merged main) but user chose NOT to merge — it awaits human review; qa-plan/dogfood/verdict checks fail on an EXPIRED repo Claude OAuth secret (also red on #2085/#2060, needs someone with secret access). PR #2089 (next 16.3.3) is code-green; user decision: 16.3.3 approved for BRANCH use only, do not merge to main yet (60-day minimum-release-age guard matures 2026-10-02). Its Vercel failure was root-caused: 16.3.3+Turbopack skips collectBuildTraces so .next/next-server.js.nft.json is missing and output:'standalone' assembly throws ENOENT on Vercel; fixed by setting output:'standalone' only when !process.env.VERCEL (Vercel ignores standalone anyway; GH CI still builds it for nightly E2E). PR #2090 (draft, root-layout TenantChrome) hit the DEV-612 conflict: root-level Suspense hides crawlable content from no-JS readers (/ 2615→67 visible chars); rework landed (a33a8f37a, CI fully green, still draft): RootLayout is synchronous and passes the getWhitelabelContext() promise down; WhitelabelProvider unwraps with use() (only it can suspend; useWhitelabel() unchanged), PrivyProviderWrapper unwraps in a PrivyTenant sibling; NO Suspense boundary anywhere in the layout, so no-JS parity is exact — but the shell is therefore still NOT prerenderable and cacheComponents stays off. KEY FACT (verified in proxy.ts:102-115): on tenant hosts, non-community routes (/about, /knowledge/*, /project/*, /funding-map...) pass through UN-rewritten with x-is-whitelabel=true, so isWhitelabel is host-derived on every route, not just community ones — any 'crawlable routes are statically non-whitelabel' shortcut is wrong. Only / (→ community funding-opportunities) and /blog (→ main site) are special-cased. CONSEQUENCE: to prerender shells the tenant must be URL-derivable = Option B: move routes under app/[tenant]/ with the root layout inside it, proxy rewrites every host to /<tenantId>/<path> internally, getWhitelabelContext reads `tenant()` from `next/root-params` (stable in 16.3.0; usable in layouts, server utilities and inside 'use cache' as a cache key; NOT in client components, server actions, route handlers, or unstable_cache; segment name must be a JS identifier; cacheComponents requires generateStaticParams with ≥1 tenant value). Route handlers (sitemaps, .well-known, api) stay outside [tenant] and the proxy must exempt them. USER APPROVED Option B on 2026-09-01. Design (brief: .maestri/briefs/instant-nav-task-4-tenant-root-param.md): internal prefix /t/<tenant>/<path> (browser URLs unchanged), root layout at app/t/[tenant]/layout.tsx, param value 'karma' for main hosts or the whitelabel config.domain for tenant hosts, public /t/* → 404 at the proxy, generateStaticParams lists all tenants, flags still OFF. Stacked integration branch feat/instant-navigations = main + #2089 + #2090; Task 4A (routes/root layout/tests) = FE Dev Alpha, Task 4B (proxy + utilities/tenant-param.ts) = Frontend Dev #2; both PRs target the integration branch as drafts. Readiness proof = throwaway cacheComponents:true build must prerender the root layout; per-route errors become Phase-2 triage input. 2026-09-01 status: integration branch feat/instant-navigations @ 6f0f5dfd1 pushed; 4A = PR #2094 (draft, green) — 510 files moved, root layout at app/t/[tenant]/layout.tsx keeps `export const dynamic = 'force-dynamic'` deliberately (removing headers() made Next try to prerender ~500 pages that fail on 47 useSearchParams callers; Phase 2 removes it per segment), route-file-structure test re-rooted to app/t/[tenant], app/global-error.tsx cannot move (resolved from app root). Readiness proof PASSED (root layout prerenders, 1477 shells = routes × tenants); remaining Phase-2 blockers: 34 incompatible segment configs (force-static route handlers, dynamic on blog/nonprofit-research/project layout, runtime=edge OG routes), 2 shell items (footer new Date(), DeferredLayoutComponents usePathname), and per-page CLIENT_HOOK_DYNAMIC from useSearchParams/usePathname. tenant-param.ts contract grew: KARMA_TENANT_PARAM + isKnownTenantParam exports; use WHITELABEL_DOMAINS; experimental.rootParams is deprecated in 16.3.3 (do not add). 4A and 4B are only deployable together; 4B = PR #2093 (green). Phase 2 triage (.maestri/reports/phase-2-triage-matrix.md): Cache 53 (= SITEMAP_NO_LOADING; 45 need no server data, 8 need 'use cache'), Block 5, Stream 103; 9 shell modules block every route (usePathname section tests in navbar/footer + footer new Date()). Phase 2 decisions 2026-09-01 (.maestri/briefs/instant-nav-phase-2-queue.md): D1 chrome route groups approved; D2 public loaders get isAuthorized:false gated on an indexer payload-parity test (api/client.ts defaults isAuthorized:true → TokenManager.getServerToken → dynamic import next/headers → cookies() on 6/8 crawlable data routes — the cache-poisoning line); D3 generateStaticParams builds only karma; D4 blog draftMode → user product decision (default: separate preview route); D5 edge OG routes spike sharp need first. Estimate ≈10–12 engineer-days. MILESTONE 2026-09-01: Task 4 complete — #2094 + #2093 merged into feat/instant-navigations @ 35a488ef0 (NOT main); all combined gates pass (public URLs unchanged on main + app.opgrants.io, /t/* 404, alias 308s intact, no-JS parity 2615/2082/7162, unit suite clean except 3 environmental flakes: vercel-build wall-clock, navbar responsive under parallel load, gsc CRLF). P2-1 DONE = PR #2096 (draft vs integration, green): (chrome) 149 routes / (bare) 12 route groups, GlobalNavbarSlot deleted, usePathname failures 2632→704; DEV-612 acceptance restated as visible-char + link-graph parity with every hidden chunk audited (the NavbarAssistantButton boundary is an accepted chunk: one button, no links); tenant hosts no longer double-navbar on find-funders (behavior fix). Merge order into integration: #2095 then #2096 (tenant-chrome/TenantFooter conflict expected). Next: Alpha on P2-3 stage 1 (D2 gate: isAuthorized:false on 4 public loaders + indexer payload-parity test, no use cache yet); TASK5 (footer year server-side, DeferredLayoutComponents usePathname, branded /t/* 404; branch feat/shell-prerender-fixes) done by Frontend Dev #2 (readiness failing routes 161→82, shell clean; footer year is a module-scope server-only constant passed as a prop; /t/* rewrites to app/t/[tenant]/blocked-internal-path which throws notFound with a colocated not-found.tsx because a parent-segment not-found.tsx does NOT catch a child page's notFound()). REGRESSION found: 4A removed the root app/not-found.tsx, so unmatched public URLs and the unknown-tenant guard render Next's unbranded 404 — FIXED in PR #2095 (draft vs integration): app/global-not-found.tsx owns its own html/body and REQUIRES experimental.globalNotFound: true in 16.3.3 (silently inert without it; a test pins the flag); it also made the /t/* sentinel route unnecessary (rewrite targets an unmatched path). P2-2 DONE = PR #2097 (green). Compiler facts learned: 'use cache' needs experimental.useCache when cacheComponents is off and only wraps async functions returning serializable values (route handlers cache the JSON body, GET wraps it); `export const instant` is a HARD BUILD ERROR until cacheComponents is on (moved to P2-6); useCache bans `runtime` exports entirely; deleting force-dynamic on sitemaps makes them build-time static (frozen URL list) — use `await connection()` instead; D5 resolved: @vercel/og on nodejs falls back to resvg-wasm, sharp NOT needed, both OG routes verified as PNGs. Left for later by design: revalidate=60 on project layout (behind D2 gate) and blog pages (P2-3), force-dynamic on the root layout (last line to delete). Quality-gate on moved files fails on stale oversizedFiles baseline KEYS (path rename) — rename the key, never widen; baseline-guard needs the quality-baseline label. Merge rule: #2095 and #2096 must land together (footer suppression), keep copyrightYear on TenantFooter. #2097 had all 6 CI shards red: cacheLife() throws unless process.env.__NEXT_USE_CACHE is set (the Next server sets it from experimental.useCache, so prod works) and vitest has no cache work-unit store, so any test importing a 'use cache' route handler throws — decision: SPLIT #2097 (flag-free half lands: connection() sitemaps/cron/token pages, OG runtime removals, karma-only generateStaticParams; 'use cache'+cacheLife conversions + experimental.useCache parked as PR #2100 on feat/use-cache-route-handlers, DO NOT MERGE BEFORE P2-6, with a next/cache vitest mock; connection() also throws outside a request scope and is mocked in the shared test setup). #2098 = P2-3A (publicReadOptions helper, D2 gate, parity fixtures; the staff-JWT live diff `INDEXER_TOKEN=<staging jwt> node scripts/record-d2-parity.mjs` is the user's to run). P2-4b slice 1 done (community hub usePathname tests removed, latent slug-prefix bug fixed); slice 2 deferred until #2095/#2096 land because the readiness export only names offenders it reaches — static reachability overstated twice, so leaf-Suspense work must be driven by the post-merge offender list. 2026-09-02: user chose to REWRITE HISTORY to strip Claude trailers (see [[no-ai-attribution-in-gap-repos]]); Rewrite DONE and independently verified: feat/instant-navigations = b785e75d3 (was 35a488ef0; tree identical, 0 trailer lines, 4 merge commits kept; pre-rewrite SHAs recorded in .maestri/reports/task-rewrite-REPORT.md). Next: Alpha rebases its 3 branches + edits PR bodies 2094/2096/2098/2099; Dev #2 rebases 2095/2097/2100/P2-4a; then merge 2095→2096→2097 with the (chrome) baseline key resolved in 2096 (Alpha chasing a CI quality-gate format-diagnostic mismatch on the merged head first — CRLF trap). P2-4a DONE = PR #2101 (ManageChromeBoundary for 28 manage routes + funding-platform searchParams into Suspense children): usePathname-outside-Suspense class 80→0; remaining failures are all uncached server reads (P2-3 stage 2 at the flip). Deferred: FundingOpportunitiesClient needs a toolbar/directory split (crawlable, DEV-596 hydration must stay in initial HTML) — assigned as its own PR; manage/payouts is a pure permanentRedirect → instant=false at P2-6. Funding-opportunities split DONE = PR #2102 (useProgramFilterUrl hook; toolbar + filtered-empty slots behind leaf boundaries; hydration test 3/3; local no-JS build had an empty directory because the local server fetch returns no programs, so the populated-directory proof must be re-run on the Vercel preview). P2-5: @next/playwright@16.3.3 hits the same 60-day minimum-release-age guard; decision = same one-time resolution override as next 16.3.3 (devDependency, .npmrc and exclude list untouched), suite skipped in CI until P2-6. STATUS 2026-09-01: integration branch feat/instant-navigations pushed @ 6f0f5dfd1 (main beafe48e1 + #2089 + #2090, both --no-ff, clean). Task 4B SHIPPED as draft PR #2093 (feat/tenant-root-param-proxy -> feat/instant-navigations): utilities/tenant-param.ts exports KARMA_TENANT_PARAM/resolveTenantParam/listTenantParams/resolveWhitelabelFromTenantParam/isKnownTenantParam plus TENANT_ROUTE_PREFIX/isTenantRoutePath/isTenantExemptPath/tenantRewritePathname; proxy.ts funnels all six page pass-throughs through one tenantRewrite() and 404s public /t/* before the whitelabel block and before the alias 308. TRAP that would have broken prod: the middleware matcher only excludes ROOT-level files with an extension, so /assets/*, /logos/*, /sitemaps/*, /.well-known/* and /monitoring (Sentry tunnelRoute) all reach the proxy and must be exempted by name; use an asset-EXTENSION allow-list, not a dot check, so /project/vitalik.eth stays a page. next.config.ts needs NO rootParams key in 16.3.3 (config.js warns experimental.rootParams is no longer needed). Verifying a proxy rewrite without the 4A route tree works: build a throwaway app/t/[tenant] layout+page+[...rest] echoing the param, next start, curl with -H Host, then delete it. Also found: utilities/auth/token-manager.ts:89 calls cookies() via a DYNAMIC import inside the function, so a grep for a static next/headers import misses it - it makes /funding-map dynamic. Canvas has duplicate 'Frontend Dev' and 'Tech Leader' terminal names — CLI asks fail on ambiguity; recruited uniquely-named 'FE Dev Alpha' as workaround. Related: [[shared-clone-for-pr-work]], [[gap-app-v2-cannot-switch-branches]], [[biome-check-cannot-pass-crlf]]. TASK 4A LANDED as draft PR gap-app-v2 #2094 (branch feat/tenant-root-param-routes @ ec1676f1c, base feat/instant-navigations @ 6f0f5dfd1 = main beafe48e1 + #2089 + #2090, both merges clean). Whole page tree git mv'd to app/t/[tenant]/; route handlers + metadata routes stay at their paths, and app/global-error.tsx CANNOT move (Next resolves global-error from the app root, next-app-loader/index.js:239-242). utilities/tenant-param.ts holds the param contract (KARMA_TENANT_PARAM, resolveTenantParam, listTenantParams, resolveWhitelabelFromTenantParam, isKnownTenantParam) — 4B owns the tested copy but must keep the last two exports. experimental.rootParams is DEPRECATED in 16.3.3 (config.js:190 says remove it) — no flag needed. parseExtraWhitelabelDomainsFromEnv is not exported; use WHITELABEL_DOMAINS. KEY CONSEQUENCE nobody predicted: with headers() gone from the shell, Next starts prerendering ~500 pages and the build FAILS — NavbarAuthButtons' useSearchParams (fixed with a Suspense boundary whose fallback is the skeleton the server already renders, so no-JS output is byte-identical) and then per-page offenders (47 modules call useSearchParams). Fix shipped: export const dynamic = 'force-dynamic' on the root layout, preserving today's rendering mode; Phase 2 deletes it segment by segment. No-JS parity verified EXACT vs #2090: 2615/2082/7162, 0 hidden chunks. Unit 15887 pass / 1 fail (gsc-site-verification, a core.autocrlf=true artefact of the scratch clone — blob is LF, checkout CRLF). READINESS PROOF (.maestri/reports/task-4-cachecomponents-readiness.md): the ROOT LAYOUT PRERENDERS under cacheComponents — no error names it, headers(), tenant() or the fonts, and the build reaches 'Generating static pages (0/1477)'. Remaining blockers: 34 files with dynamic/revalidate/runtime segment configs incompatible with the flag (all .well-known + sitemap route handlers, blog, nonprofit-research token pages, project layout, 2 edge-runtime OG routes), 2 shell items (footer.tsx:54 new Date(), DeferredLayoutComponents.tsx:77 usePathname), and a per-page CLIENT_HOOK_DYNAMIC tail. experimental.missingSuspenseWithCSRBailout no longer exists in Next 16, so the build bails at the FIRST bad route — enumerating the tail needs route-by-route work, not one sweep. 'use cache' on getWhitelabelContext alone did NOT clear the WhitelabelProvider error, and the code frame Next prints there is the nearest named owner, not the real offender. Tech Leader ambiguity persists (2 terminals named 'Tech Leader'), so reporting goes to .maestri/reports/task-4a-REPORT.md plus the canvas note 'TASK4A Integration Branch'.

2026-09-02 INCIDENT: concurrent full vitest suites + next builds by both devs froze the user's machine; FE Dev Alpha's terminal died mid merge-train (local merge in %TEMP%\sg	4a, never pushed; 15/16 line-ending format fixes possibly recoverable). Alpha restarted in place with .maestri/briefs/instant-nav-alpha-restart-brief.md (resource rules first; resume merge 2095→2096→2097, typecheck only, push, then WAIT for an explicit go before any readiness build). Dev #2 (running, all processes killed) does no-build work: instant() suite typecheck + draft PR, #2102 preview no-JS proof via HTTP only. Builds are now serialized by the Tech Leader, one at a time. See [[local-test-runs-freeze-machine]].

MILESTONE 2026-09-02: merge train had already been pushed before the freeze — feat/instant-navigations = a0822acca = b785e75d3 + #2095 + #2096 + #2097 (all MERGED into the integration branch, not main; conflict rules verified; 16/16 line-ending fixes; 0 format diagnostics; tsc clean; no attribution). NOTE: the integration branch itself gets NO CI runs (workflows trigger on pull_request and push-to-main only) — signal exists only on PRs targeting it. Next: Alpha rebases #2098/#2099 onto a0822acca, then ONE approved readiness build (report task-p2-readiness-a0822acca.md).

Status 2026-09-02 midday: PRs against integration: #2098 (public loaders no auth, rebased e5a6aa196), #2099 (community hub cleanup, rebased dbd1ffd10), #2100 (use-cache handlers, parked), #2101 (manage/admin leaf Suspense), #2102 (funding-opportunities toolbar split, preview-proven), #2103 (instant() Playwright suite, skipped in CI, @next/playwright via one-time override), #2104 (route-structure premise rewrite). Quality-gate biome drift fixed at source: one organizeImports slip in a file #2096 added; commit 8151558d9 on feat/instant-navigations (new tip), baseline stays 1151. Lesson: compare NON-format biome diagnostics between merge-base and tip (format counts are meaningless on a CRLF tree). Readiness build on tip 8151558d9 approved (one build, cpus:2 + staticGenerationMaxConcurrency:2). Dev #2 preparing feat/cache-components-flip (stacked on #2100: flags on, drop experimental.useCache, remove 23 segment configs incl. root force-dynamic, instant=false on 6 Block routes) — code-only, no push/build until go. Pre-existing finding parked for the user: /communities renders no community links without JS (finding-communities-cards-no-js.md).

READINESS MILESTONE 2026-09-02 (task-p2-readiness-8151558d9.md): shell prerenders; 350 pages attempted (karma-only), 273 prerendered, 77 failing — ONE class only (uncached data outside Suspense). 46/53 Cache-class routes prerender; the 7 failing = projects, funding-map, community hub, funding-opportunities, whitelabel programs/[programId], project profile (all need #2098 + 'use cache' on their loaders = P2-3 stage 2) and blog/[slug] (draftMode → default decision: separate blog/preview/[slug] with instant=false). Other 70: 68 Stream routes (community/** 50, project/** 12, nonprofit-research 4, nonprofits 4) needing per-route Suspense, 2 token routes needing instant=false. Assigned: Alpha → merge #2098/#2099 into integration if green, then feat/cache-loaders (stacked on the flip branch), then feat/blog-preview-route; Dev #2 → flip branch push as DO NOT MERGE draft for remote CI/Vercel signal.

PR #2105 = feat/cache-components-flip @ 7734fb94a (draft, 'DO NOT MERGE (P2-6 flip)'), stacked on #2100 rebased to 7056c6a84 on 8151558d9: cacheComponents + partialPrefetching on, experimental.useCache dropped, globalNotFound kept, 0 rejected segment configs, 6 instant=false, 7 maxDuration kept. Exists to be built remotely by CI/Vercel; a red build is its deliverable. Alpha's feat/cache-loaders and feat/blog-preview-route stack on it.

MERGE3 2026-09-02: feat/instant-navigations tip = a2ac4b958 (= 8151558d9 + #2098 + #2099, both CI-green after the baseline fix, no conflicts). #2100/#2105 being rebased onto it by Dev #2; Alpha's feat/cache-loaders (P2-3 stage 2) and feat/blog-preview-route then stack on the rebased #2105 head as drafts against the flip branch, built remotely.

FIRST REMOTE cacheComponents BUILD (PR #2105 on Vercel, 2026-09-02): compiles, 350 pages, then dies on the first route with a dynamic segment — stack = html→body→PrivyProviderWrapper→privy-bridge-context→WhitelabelProvider use(promise) (utilities/whitelabel-context.tsx:41). Pattern: routes with no dynamic segment beyond [tenant] prerender; routes with [communityId]/[projectId]/[slug]/[id] make params runtime data and the provider suspends outside Suspense (which DEV-612 forbids adding). Fix direction approved: getWhitelabelContext() becomes a 'use cache' function keyed by tenant() (pure function of the root param). Also: experimental.prerenderEarlyExit defaults TRUE in 16.3.3 (one failing route per build) — set false on the flip branch as a temporary diagnostic; GitHub Actions does NOT run next build on PRs (build job skips), Vercel is the only real build.

SECOND REMOTE BUILD (2105 @ 822afcb7d, cached getWhitelabelContext + prerenderEarlyExit:false): shell FIXED, 86/161 prerender, 75 fail, footer/usePathname causes gone. Cause A (65): layouts awaiting params at top level — community/[communityId]/layout.tsx (52 routes) and project/[projectId]/layout.tsx (13); loading.tsx does not rescue (read is above page boundaries); manage/payouts passes via instant=false. Cause B (10): page-level — nonprofits/find-funders/*/[id] x4, nonprofit-research/[reportId], personas/[handleId] (Stream: params into Suspense child) and blog, blog/[slug], funding-map, projects (uncached loaders = P2-3 stage 2). Decision: generateStaticParams samples on the two dynamic layouts + blog/[slug] (layouts may then keep top-level await params; runtime values render on first request and persist) + 'use cache' on their loaders — assigned to Alpha as TASK-P2-6A (feat/cache-loaders vs the flip branch). Every remaining error prints the whitelabel-context.tsx:41 frame (outermost client frame while a server ancestor suspends — inference, not proven; next build --debug-prerender would settle it).

P2-6B RESULT (dpl_EXvRBhz9v53dEexUSWnAFZxF1vv6, read via my Vercel connector): still 75 failing; all six Stream detail pages still fail with the same whitelabel-context.tsx:41 client frame → page-level Suspense on params did not reach the real reader; the '86 pass' set is exactly the no-dynamic-segment routes, so the cached getWhitelabelContext may have changed nothing (build 1 early-exited). Next diagnostic: --debug-prerender on the flip build (temporary) for true server stacks; Dev #2 also fixing generateMetadata params reads in 5 of the 6 files. Dev #2 cannot read Vercel logs (CLI unauthenticated, MCP banned) — the Tech Leader reads them with mcp__claude_ai_Vercel__get_deployment_build_logs (teamId karma-devs) and relays. Alpha's #2108 (generateStaticParams samples + cached loaders + cached RQ seeds) build pending: dpl_BJp7rZp6cjKZ6BT51ihYwwB2S9cM.

THIRD REMOTE BUILD (#2108 @ 80847fd1c, generateStaticParams samples + cached loaders + cached RQ seeds): 69 paths failing (project samples ×3); blog, blog/[slug] and 25/28 manage routes CLEARED. NEW DOMINANT CLASS with a real stack: 'unstable value Date.now() in a Client Component' = React Query useQuery itself (hooks/useProject.ts:12 via useProjectProfile in ProjectProfileLayout) rendered during prerender above crawlable content; routes whose useQuery consumers sit under leaf Suspense pass, crawlable routes that render them above content fail and DEV-612 forbids the boundary. funding-map/projects/community hub likely same class. Spike assigned (TASK-RQ-SPIKE): (a) staleTime 'static'/Infinity on hydrated queries to dodge QueryObserver's Date.now(), else (b) server-render crawlable content from the cached loader and push useQuery consumers into leaf boundaries below content. Log digests the Tech Leader writes for devs: .maestri/reports/vercel-log-<pr>-<sha>.md.

RQ SPIKE (a) finding: @tanstack/react-query's QueryObserver returns before isStaleByTime/Date.now() when resolveStaleTime(...) === 'static', so `staleTime: 'static'` structurally avoids the prerender clock read; applied OPT-IN as a `prerenderSafe` option through useProjectProfile, passed only by ProjectProfileLayout (the one consumer above crawlable content). Caveat: a static query never refetches on mount/focus/reconnect and ignores invalidation for staleness — only acceptable where the server document is already cache-revalidated; never on user-editable views. Alpha's own Vercel connector works (the hang was Dev #2's process) — Alpha reads/digests its own build logs now.

FOURTH REMOTE BUILD (#2108 @ 20b6b167d): 69→48 paths; staleTime 'static' cleared all non-nested project profile routes. Remaining: Dev #2's six; funding-map/projects (prerenderSafe extension pending); community hub/funding-opportunities/programs/[programId] (Cache) and ~13 community Stream routes; manage routes with a further unknown segment ([programId]/[reportId]) fail on ManageLayoutClient useParams() (CLIENT_HOOK_DYNAMIC) → pass communityId as a server prop; project funding/[grantUid]/* nested routes (GrantDetailLayout suspect). Digests: .maestri/reports/vercel-log-2108-<sha>.md.

P2-6B2 (Dev #2, source-verified): generateMetadata CANNOT block the build shell — the export worker sets serveStreamingMetadata=true so metadata renders inside a Suspense boundary (next/dist/export/worker.js:186, lib/metadata/metadata.js:104-121); fetch with an explicit revalidate IS cached under cacheComponents (patch-fetch.js:151/767), converting to 'use cache' would only cache failures. The six detail pages fail because of usePathname() in section layouts ABOVE the page: NonProfitsNavbar isHomepage (crawlable find-funders section → fix by route-tree split, no Suspense) and DonorResearchSectionLayout token/index/shellless gating (→ server layouts pass an explicit mode prop). --debug-prerender enabled on PR #2107 @ 38e9dea11 via NEXT_BUILD_ARGS in vercel-build.sh (temporary). vitest 4 dropped --poolOptions.forks.maxForks: use --pool=forks --maxWorkers=2.

DEBUG-PRERENDER BUILD (#2107 @ 38e9dea11): exact server frames confirm every hypothesis (digest vercel-log-2107-38e9dea11-debug.md). New precise finding: app/t/[tenant]/(chrome)/projects/page.tsx:34 awaits searchParams at top level — fix = default list prerendered from the cached loader outside any boundary + searchParams read in a leaf Suspense child that swaps in the filtered list (toolbar-split shape). Keep --debug-prerender on the flip branch until zero failures.

Alpha extension round (pushed, build pending): clock-free staleTime placed directly on use-funding-programs and useProjectsExplorerInfinite (module-confined to Cache-class routes = scoping by module boundary, accepted deviation); projects page no longer awaits searchParams (default list prerendered; ProjectsExplorer reconciles nuqs URL state client-side; orchestration test rewritten to the new contract); GrantDetailLayout takes projectId/grantUid as server props; --debug-prerender added on feat/cache-loaders. Lesson: per-file `biome check --stdin-file-path` does NOT surface noUnusedImports — only a repo-wide capture matches the CI gate. Open: FundingOpportunitiesClient useParams/usePathname → params-as-props (assigned). Next housekeeping decision: merge #2101–#2104 into integration and rebase the flip stack before the final flip PR.

FIFTH BUILD (#2108 @ a67784df8, debug-prerender): 48 unchanged — extension round cleared nothing; inference-based fixes hit the wrong readers. Real frames (digest vercel-log-2108-a67784df8-debug.md): nested manage → ManageLayoutClient.tsx:8 useParams(); (cover) financials/reports → (cover)/layout.tsx:23-24 await params; nested grant routes → ProjectProfileLayout.tsx:107 useParams()/:108 usePathname() (NOT GrantDetailLayout); nonprofit-research → DonorResearchSectionLayout:168. Rule adopted: fix ONLY from a named frame; pull the frame first. Frames for funding-map/projects/hub/funding-opportunities/programs/[programId] still to be pulled by Alpha with time windows.

CONSOLIDATION 2026-09-02 (afternoon): Dev #2 6B3 done (manage communityId prop through ManageLayoutClient+ManageLayoutShell; find-funders (landing-nav)/(workbench-nav) groups; donor-research (advisor)/(gated-fullscreen)/(public) + bare token layouts name a mode). KEY FACT: api.* loaders are axios (utilities/api/client.ts) so Next fetch-cache options (next: {revalidate}) NEVER apply to them; only 'use cache' works. Alpha's frame pull: ONE frame in community (with-header)/layout.tsx explains hub, funding-opportunities, impact, updates, community projects, browse-applications (one-line fix, pushed first); programs/[programId] and projects frames not yet captured (projects hypothesis: nuqs useQueryState in ProjectsExplorer above the list); project updates still reaches cookies() via raw getProjectUpdates; FundingOpportunitiesClient useParams is hygiene, not the blocker. Sequence: with-header one-liner → merge #2108 into flip → merge #2104 (green) and #2101-#2103 after Dev #2 rebases them onto a2ac4b958 (quality-gate red = phantom +1) → rebase #2100/#2105 → Dev #2 rebases #2107 and moves cover/with-header layouts to the cached community twin.

SIXTH BUILD (#2108 @ 2c0c2e9f7, WithHeaderLayout → getCommunityDetailsCached): the six (with-header) routes STILL fail — a further reader exists in that chain (Community Header / CommunityPageNavigator suspects); frame to be pulled before any fix. Quality-gate lesson #2: knipUnusedTypes is a separate metric (ProjectsExplorerSearchParams went unused when projects stopped reading searchParams) — read the CI run, never reconstruct locally. Consolidation underway: 2108→flip, 2104+2101-2103→integration (all green after Dev #2's rebase onto a2ac4b958), then 2100/2105 rebased, then Dev #2 rebases 2107.

ALL FRAMES NOW NAMED (2026-09-02 ~18:15Z): with-header chain moved from the layout read (fixed by getCommunityDetailsCached) to NormalCommunityHeader (components/Community/Header.tsx:58 useParams()) → communityId prop; programs/[programId] page top-level await params without sample → generateStaticParams programId sample; projects → ProjectsExplorer.tsx:50 nuqs useQueryState above the list → toolbar-slot split; nested grant routes → ProjectProfileLayout.tsx:107 useParams(); nested manage → ManageLayoutClient.tsx:8; (cover) → uncached axios getCommunityDetails in (cover)/layout.tsx; nonprofit-research → DonorResearchSectionLayout:168 (fixed on Dev #2's branch). Vercel log timestamps are UTC; time-windowed errors-only pulls (since/until ISO Z) reach frames the tail window misses. Per-frame progress, not the total count, is the progress signal (a route leaves the list only when every frame in its chain is clear).

CONSOLIDATED 2026-09-02 ~18:20Z (verified): feat/instant-navigations = a3603d882 (a2ac4b958 + #2104 + #2101 + #2102 + #2103, all MERGED into integration); #2100 and feat/cache-components-flip (#2105) rebased onto it; flip head = 5a089bffa = feat/cache-loaders head (#2108 temporarily empty; Alpha's four frame-backed fixes refill it: NormalCommunityHeader prop, programs/[programId] generateStaticParams sample, ProjectProfileLayout props, ProjectsExplorer toolbar split). Dev #2 rebasing #2107 onto 5a089bffa then (cover) callers → cached twin + remaining community Stream frames. Consolidated flip build (dpl 6P6Es8VJmu37AGGFGAqDQSq1eBKZ): 48 paths, unchanged as expected.

CONSOLIDATED FLIP FRAMES (dpl 6P6Es8VJmu37AGGFGAqDQSq1eBKZ, digest vercel-log-2105-6b0829546-tl-frames.md): hub/impact/project-discovery/community projects/updates → CommunityPageNavigator.tsx:120 useSearchParams (+useParams :119, usePathname :122) via NormalCommunityHeader:272 — Alpha's chain (props from the server layout; searchParams read into a link-free leaf; the navigator's links are crawlable). donate ×3 → donate/layout.tsx:27-29 uncached getCommunityDetails → cached twin (Dev #2). project/[projectId]/updates → raw getProjectUpdates reaching cookies() (Alpha). Dev #2 decisions: financials = hoist h1 out of PublicControlCenter, never cache payouts/KYC seed; impact route-tree split NOT needed (no frame names the impact layout).

FRAME MAP COMPLETE (vercel-log-2105-6b0829546-tl-frames.md, rewritten): Alpha owns Header.tsx:58 prop, CommunityPageNavigator.tsx:120 (props + link-free leaf; covers hub/impact/project-discovery/projects/updates/browse-applications/funding-opportunities), programs/[programId] samples, ProjectProfileLayout.tsx:107 props, ProjectsExplorer.tsx:50 split, funding-map use-funding-filters.ts:77 nuqs split (FundingMapSearch/FundingMapList), project updates cached twin. Dev #2 owns donate/layout.tsx cached twin + financials h1 hoist; its #2107 already carries manage prop, cover cached callers, find-funders route groups, donor-research mode groups. Warning-level cleanup later: raw getCommunityPrograms (community-programs.service.ts:22) reaching cookies() on manage/portfolio-reports/config and manage/milestones-report.

DEV #2 BUILD (#2107 @ 8174b8d9a, dpl EB7jSs1bWEZNzyF4T5AJeaDUZxff): 48 → 31 paths. Cleared: find-funders/[id] ×4 (route groups), nonprofit-research ×2 (mode prop), nested manage ×9 (communityId prop), financials, reports index (cached twin). Remaining 31: Alpha's chain (hub/impact/project-discovery/projects/updates/browse-applications ×2/funding-opportunities), programs/[programId], funding-map, projects, grant routes ×15, plus Dev #2's donate ×3 and reports/[runDate] ×2 (frame pending).

SHARED-INFRA FRAME: hooks/use-url-builder.ts:23 useParams() inside src/components/navigation/Link.tsx:22 — every app Link calls useParams; blocks routes with an unknown nested param ([runDate], [grantUid], [programId], [referenceNumber]) where a Link renders above content (build-time-sample params mask it elsewhere). Fix (Dev #2): community slug from a client context provided by the server community/[communityId]/layout.tsx, targetCommunity overrides, useParams call removed (a hook call is flagged even if unused). Alpha told not to touch those files.

NEXT INTERNALS RULE (Alpha, next/dist/client/components/navigation.js): useSearchParams() → useDynamicSearchParams aborts a cacheComponents prerender UNCONDITIONALLY; usePathname() and useParams() → useDynamicRouteParams abort ONLY when a route param is not a build-time sample. Hence: any useSearchParams above content must move into a leaf; useParams/usePathname above content are fine on sampled routes but block routes with an unknown nested param ([grantUid], [programId], [referenceNumber], [runDate]) — answer = generateStaticParams sample for that segment or move the read into a link-free leaf. useSelectedLayoutSegment() also calls useDynamicRouteParams (no escape hatch).

HEURISTICS: sample (generateStaticParams) only small known id spaces ([grantUid] of sampled projects, [programId] of the sampled community, [referenceNumber]); unbounded user-created ids ([reportId], [handleId], [id], [runDate]) need structural fixes (props/route groups/removed hooks). nuqs explorers (funding-map, projects): the LIST reads the hooks too (FundingMapList:48, ProjectsExplorer drives its query from the same hooks), so the split = list renders from a server default with no nuqs read, controls own the reads behind a leaf, state flows list-ward via store/callback — two sizeable refactors, Alpha's next unit after the samples push. Dev #2 (2107 @ 24005bcf7): donate ×3, financials hoist (PublicControlCenter reads useSearchParams so the boundary was mandatory), reports/[runDate] ×2 via the Link/useUrlBuilder context fix; now removing the six redundant P2-6B page boundaries + getCommunityPrograms publicReadOptions cleanup. Next: merge 2107 into the flip once green, Alpha rebases.

DEV #2 BUILD (2107 @ 24005bcf7, dpl 6HAejMVYmSMTDV4s5gH3WwYCX71K): 31 → 26 paths; donate ×3 + reports/[runDate] ×2 cleared; Dev #2's lane fully clear. Remaining 26 all Alpha's: header chain ×9 (hub, funding-opportunities, impact ×2, community projects, updates, browse-applications ×2, programs/[programId]), funding-map, projects, grant routes ×15. Next: 2107 merges into the flip; Alpha rebases and pushes samples/header/profile-layout build; then the two nuqs splits.

Decision: browse-applications/[referenceNumber] is a redirect-only page (permanentRedirect) → `export const instant = false` (precedent: manage/payouts); never add a generateStaticParams sample to a redirect-only route. Alpha pushed samples (grantUid, programId) + Header prop + navigator + ProjectProfileLayout props + updates cached twin; judging Link-touching frames only after rebasing onto the flip with Dev #2's use-url-builder fix. Next unit: funding-map nuqs split, then ProjectsExplorer.

FLIP HEAD 2d8e25dc0 (2026-09-02 ~19:00Z): 2107 merged (--no-ff) into feat/cache-components-flip — Dev #2's find-funders/donor-research/manage/cover/donate fixes + Link/use-url-builder context fix + six redundant P2-6B boundaries removed + getCommunityPrograms publicReadOptions (indexer route is intentionally public: community.routes.ts:259-286). Alpha rebasing feat/cache-loaders onto it; remaining 26 paths all Alpha's. Dev #2 writing .maestri/briefs/instant-nav-p2-6-checklist.md (flip merge checklist).

CONFIRMED (2107 @ c6e766eeb, dpl M6q188w4XWpakZyUp3s51tPx937r): still 26 paths after removing the six redundant P2-6B page boundaries and the getCommunityPrograms cleanup — the six Stream detail pages stay clear without boundaries (their blockers were the section layouts). Remaining 26 are exactly Alpha's list.

Alpha @ 376c88c1d (rebased on flip 2d8e25dc0): funding-map split done — list reads a context seeded with the page's prefetched default (prerendered HTML = default program page), FundingMapUrlState leaf behind Suspense owns all URL reads and publishes params back down; pagination/filters are buttons not links (no link-graph loss); sidebar Link stays outside boundaries; FundingMapList.test mocks the context. Only ProjectsExplorer split remains on the frame list (toolbar must be extracted from the 379-line component first).

FLIP MERGE CHECKLIST written (.maestri/briefs/instant-nav-p2-6-checklist.md, from flip 2d8e25dc0). Flags: (1) remove prerenderEarlyExit:false, --debug-prerender AND the vercel-build.sh ceiling in one commit (half-removal = many failures, no causes); (2) ONLY app/api/blog/revalidate/route.ts calls revalidateTag — community/project/program cached reads have tags but nothing fires them → non-blog surfaces are time-based only (60s/minutes): LAUNCH DECISION for the user (default: accept for launch, add mutation/webhook-driven revalidateTag as follow-up); (3) every generateStaticParams degrades to [] silently — confirm samples actually prerendered on the flip preview; (4) instant() suite uses skipUnlessFound — read the skip count, not the exit code.

GOTCHA: merging a stacked branch into its base PR branch auto-MERGES its GitHub PR (2108 went MERGED at 2c0c2e9f7 when feat/cache-loaders was merged into the flip); later pushes to that branch have no open PR and Vercel does NOT build them (only PR branches build). Re-open a draft PR after every such merge before judging a build. Alpha's 5a089bffa/376c88c1d were never built; new PR being opened.

NEW PR #2111 = feat/cache-loaders (2108 auto-merged). Build @ 376c88c1d died at page-data collection: cacheComponents requires EVERY generateStaticParams to return ≥1 result; the [grantUid] layout sample returned [] (sampled projects had no grants / lookup failed) → hard build error 'empty-generate-static-params'. Rule: samples derive from live data that guarantees ≥1 (projects with grants), with a checked-in list of real stable uids as fallback pinned by a parity-style test; never fabricated, never [].

PR #2111 @ 531766bfd (dpl E1FKec3dat1qH2TkFHbDYjqUVj8D): 26 → 9 paths. Cleared: all grant routes, impact ×2, community projects, updates, browse-applications ×2. Remaining unique routes: projects (ProjectsExplorer split pending), funding-map, hub, funding-opportunities, programs/[programId] (now with real samples), project/[projectId]/updates ×3 samples — frames being pulled.

FINAL 9-PATH FRAMES (digest vercel-log-2111-531766bfd-tl-frames.md): hub (with-header)/page.tsx:64 top-level await searchParams → default page-1 list prerendered, ?page in a leaf; funding-opportunities use-programs.ts:48 useQuery Date.now() → staleTime static opt-in; programs/[programId] page.tsx:191 setQueryData server-side → cached seed; project updates page.tsx:53 prefetchQuery → cached seed; funding-map funding-map-card.tsx:71 new Date() in client render (hasEnded) → defer/pass from server; projects nuqs split pending. Warning: grant layout generateMetadata getProjectGrants authorized → publicReadOptions.

PR #2112 (Dev #2, feat/verify-flip-preview vs integration): scripts/verify-flip-preview.mjs + scripts/indexability/ library — classifies sample routes from x-nextjs-prerender / x-vercel-cache / cache-control (MISS ≠ dynamic; conflicting signals = unknown, never a regression; TTFB reported, never decides), sample values from the deployment's own sitemap, program/grant samples declared:false until Alpha's land; runs the DEV-612 extractors on the 53 crawlable routes + whitelabel spot checks; exits non-zero on regression; 46 fixture tests; manual-only like crawl-sitemap.

PR #2111 @ 065cd402c (dpl GbqJMAjnBEDfBMqA4GEDVZnCL33c): 9 → 5 paths (4 unique routes): projects (explorer split) and project updates (cached seed) cleared. Remaining: programs/[programId] ×2 samples, funding-opportunities, community hub, funding-map. Grant-route generateMetadata getProjectGrants warnings persist (publicReadOptions pending).

FRAMES @ 065cd402c (digest vercel-log-2111-065cd402c-tl-frames.md): hub → cookies() INSIDE 'use cache' via getCommunityCategoriesOrThrow (getCommunityData.ts:180) called from the cached loader (:231) — D2 class: publicReadOptions on every api.* call reachable inside cache scopes; programs/[programId] → useProgram use-program.ts:17 useQuery (staleTime static opt-in); funding-opportunities → usePrograms:50 still uncovered by the opt-in; funding-map → funding-map-card.tsx:44 isPendingReview new Date() (second occurrence).

D2 GUARD assigned to Dev #2: a structural vitest that walks every 'use cache' function (and cached seed) transitively and fails on any api.* call reachable without publicReadOptions()/isAuthorized:false — the ratchet for the cache-poisoning line (the hub's getCommunityCategoriesOrThrow slipped past the four-loader gate).

PR #2113 (Dev #2, feat/use-cache-auth-guard vs integration): static call-graph test from every 'use cache' function to any api.* call not provably anonymous (carries the literal isAuthorized:false fact through forwarded options; follows cache() wrappers, re-exports, object-literal services; stops at package boundaries). One true offender: getCommunityCategoriesCached → getCommunityCategoriesOrThrow (getCommunityData.ts:180, no options). KNOWN_OFFENDERS ratchets both directions; entry to be deleted when Alpha's fix lands.

PR #2111 @ a402088ce (dpl 4kqVj2S1TikQUokMzsAFHDd3pr89): 5 → 4 paths (3 routes): funding-map cleared. Remaining: programs/[programId] ×2 samples, funding-opportunities, community hub. Grant-route generateMetadata getProjectGrants warnings persist.

FRAMES @ a402088ce (digest vercel-log-2111-a402088ce-tl-frames.md): programs/[programId] → utilities/funding-programs.ts:78 new Date() in client render; hub → hooks/useProjectFilters.ts:17 nuqs in CommunityGrants (third toolbar split); funding-opportunities → use-programs.ts:13 matchesStatus new Date() in a render-time filter. Instruction: sweep all Cache-class render paths for new Date(/Date.now( in one push.
